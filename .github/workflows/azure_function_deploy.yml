name: Build and Deploy Azure Function

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        run: echo "f?!>c3]4Jp@Nd:`G}QYuS6m(nZFsC/X2" | docker login -u "baktybek" --password-stdin

      - name: Build and push Docker image
        run: |
          docker build -t baktybek/azure-functions:latest -f myfunction/Dockerfile .
          docker push baktybek/azure-functions:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure
        uses: azure/functions-container-action@v1
        with:
          app-name: bahaFunctionApp
          image-name: baktybek/azure-functions:latest
          registry-url: docker.io
          registry-username: baktybek
          registry-password: f?!>c3]4Jp@Nd:`G}QYuS6m(nZFsC/X2
